# Task 05 · SQLite 数据模型

## 目标
设计并实现资产库使用的 SQLite 数据模型与访问层，支持高并发读写与全文检索需求。

## 关键输出
- 数据表结构定义（资源库、资产、标签、全文检索、AI 缓存等）。
- 数据访问封装与事务策略，含批量写入优化。
- 数据迁移与版本升级方案。

## 验收标准
- 能够创建并初始化数据库文件，生成所需表结构与索引。
- 封装的 DAO/Repository 层提供常用查询、写入、批量操作接口。
- 对数据库文件路径自定义、备份与恢复提供接口支持。
