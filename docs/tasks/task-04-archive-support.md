# Task 04 · 压缩包资产解析

## 目标
实现对常见压缩格式的读取与索引能力，在扫描阶段识别压缩包内资产，并在预览时按需抽取文件。

## 关键输出
- 多格式压缩包读取适配层及错误处理策略。
- 压缩包内资产索引模型与逻辑路径表示。
- 预览阶段的临时解压与缓存管理机制。

## 验收标准
- 支持 zip/rar/7z/tar.gz 等主要压缩格式的索引与按需抽取。
- 列表中可显示压缩包来源标识，详情预览能成功读取内部文件。
- 超大压缩包具备性能兜底策略（如大小阈值、用户提示或分批读取）。
