# Task 07 · 标签管理与 AI 辅助

## 目标
提供完善的标签管理功能并集成 AI 辅助标签与中文翻译生成流程，支持批量处理与结果缓存。

## 关键输出
- 标签 CRUD 接口、前端管理界面与数量上限校验。
- AI 调用模块、缓存策略以及批处理任务调度。
- 用户提示与错误兜底设计。

## 验收标准
- 用户可手动新增、编辑、删除资产标签，并受到数量上限约束。
- 配置有效的 API Key 后，可触发 AI 生成标签与中文译名，并可查看处理进度。
- AI 结果缓存避免重复请求，错误情况下提供提示或降级处理。
